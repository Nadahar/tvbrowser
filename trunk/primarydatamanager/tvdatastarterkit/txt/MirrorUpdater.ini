
# The FTP settings of your mirror
#
# NOTE: For security reasons the MirrorUpdater does not create the
#       target directory (path). Please do this by hand.
dataTarget.url = ftp://www.mydomain.de
dataTarget.path = download/tvdata
dataTarget.user = someuser
dataTarget.password = verysecret

# The weight of your mirror (100 = normal, 0 = the mirror won't be used)
mirror.weight = 100

# The IDs of the channel groups that should be stored on the server
groups = main:local:others
